<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title }} | Reflections</title>
  <link href="https://fonts.googleapis.com/css2?family=EB+Garamond&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <button class="dark-toggle" onclick="toggleDarkMode()">🌙</button>

  <header>
    <h1>Reflections</h1>
    <nav>
      <a href="/index.html">Home</a>
      <a href="/about.html">About</a>
      <a href="/posts.html">Posts</a>
      <a href="/tags.html">Tags</a>
      <a href="/newsletter.html">Newsletter</a>
    </nav>
  </header>

  <main style="max-width: 800px; margin: 3rem auto; padding: 0 1rem;">
    <article class="post">
      
      <!-- Show draft badge if this post is marked as draft -->
      {% if draft %}
        <p style="color: red; font-weight: bold;">⚠️ Draft Post</p>
      {% endif %}

      <h2>{{ title }}</h2>
      <p class="meta">{{ date | date }}</p>
      
      <!-- Display featured image if present -->
      {% if featured_image %}
        <div class="post-featured-image">
          <img src="{{ featured_image }}" alt="{{ title }}" style="max-width: 100%; height: auto; margin: 1rem 0;">
        </div>
      {% endif %}

      <!-- Main post content rendered safely as HTML -->
      <div class="post-body">
        {{ content | safe }}
      </div>

      <!-- Display tags if any exist -->
      {% if tags %}
        <div class="post-tags" style="margin-top: 1rem;">
          <strong>Tags:</strong>
          <ul style="list-style: none; padding: 0; display: flex; flex-wrap: wrap; gap: 0.5rem;">
            {% for tag in tags %}
              <li style="background: #eee; padding: 0.3rem 0.6rem; border-radius: 12px;">#{{ tag }}</li>
            {% endfor %}
          </ul>
        </div>
      {% endif %}

    </article>
  </main>

  <footer>
    <p>&copy; 2025 Soch Se Shabd Tak</p>
  </footer>

  <script>
    // Apply dark mode on page load if previously enabled
    if (localStorage.getItem("dark-mode") === "enabled") {
      document.body.classList.add("dark-mode");
    }

    // Toggle dark mode and save preference in localStorage
    function toggleDarkMode() {
      document.body.classList.toggle("dark-mode");
      if (document.body.classList.contains("dark-mode")) {
        localStorage.setItem("dark-mode", "enabled");
      } else {
        localStorage.setItem("dark-mode", "disabled");
      }
    }
  </script>
</body>
</html>
